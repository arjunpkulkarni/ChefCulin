# Client-side OpenAI (Vite exposes only VITE_* vars to the browser)
VITE_OPENAI_API_KEY=
VITE_OPENAI_MODEL=gpt-4o-mini

# Optional: override artifact API base (default proxies /api → :8001)
# VITE_API_BASE=/api
